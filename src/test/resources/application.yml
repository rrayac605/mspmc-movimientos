server:
    port: 8037
                     
logging:
  #file: ${PATH_LOGS}/${spring.application.name}.log
  file: ${spring.application.name}.log
  pattern:
    console: "%d %-5level %logger : %msg%n"
    file: "%d %-5level [%thread] %logger : %msg%n"
  level:
    org.springframework.web: INFO
    mx.gob.imss.cit.tooad: DEBUG
    com.netflix: WARN
    
    
spring:
  data:
    mongodb:
      authentication-database: admin
      #username: ${usrMongo} 
      #password: ${pwMongo}
      database: test
      port: 27017
      host: localhost
  application:
   name: mspmc-movimientos